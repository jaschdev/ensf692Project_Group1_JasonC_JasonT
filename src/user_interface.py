"""
user_interface.py

Provides a simple terminal-based user interface for interacting with the dataset.

Responsibilities:
- Prompt user for at least two search/filter criteria
- Provide clear instructions and feedback via print statements
- Validate user input using try/except
- Loop/re-prompt until valid input is received
- Return user selections to main.py for further processing

Note:
All user input must be dynamic (based on DataFrame contents), not hard-coded.
"""
