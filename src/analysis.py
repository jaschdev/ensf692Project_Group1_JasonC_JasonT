"""
analysis.py

Performs data analysis and manipulation operations on the dataset.

Responsibilities:
- Use describe() to show aggregate stats
- Add at least two new columns to the dataset
- Perform at least one groupby operation
- Use masking to filter subsets of data
- Perform an aggregation computation on a subset
- Create and print a pivot table
- Include at least two user-defined functions or a class with two methods

Note:
All transformations must apply to the DataFrame returned from data_loader.py.
"""
